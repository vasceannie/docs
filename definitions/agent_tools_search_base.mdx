# base

[code:agent/tools/search/base.py]

## Content

```python
"""Base search functionality

This module provides base classes, protocols and models for the search functionality.
"""

import abc
import asyncio
import time
from collections import defaultdict
from dataclasses import dataclass, field
from functools import wraps
from types import TracebackType
from typing import (
    Awaitable,
    Callable,
    ClassVar,
    Iterator,
    Literal,
    MutableMapping,
    Protocol,
    TypeVar,
    cast,
    runtime_checkable,
)

from agent.exceptions import S...
```