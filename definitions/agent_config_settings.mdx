# settings

[code:agent/config/settings.py]

## Content

```python
"""Environment and default settings.

This module provides functionality for environment and default settings
in the agent framework.
"""

import logging
import os

from dotenv import load_dotenv

# Local imports
from agent.constants import (  # noqa: F401
    BRAVE_NEWS_ENDPOINT,
    BRAVE_SEARCH_ENDPOINT,
    BRAVE_SUMMARIZER_ENDPOINT,
    BRAVE_WEB_ENDPOINT,
    DEFAULT_LANGSMITH_ENDPOINT,
    DEFAULT_LANGSMITH_PROJECT,
    DEFAULT_POSTGRES_DB,
    DEFAULT_POSTGRES_HOST,
    DEFAULT_POSTGRES_...
```