---
title: 'agent/config/settings.py'
description: 'API reference for agent/config/settings.py'
sidebar_label: 'Settings'
icon: 'gear'
---

# Settings Module

## Imports

```python
# Original imports
import logging
import os
from enum import Enum
from typing import Any, Dict, List, Optional
from dotenv import load_dotenv
from agent.constants import BRAVE_NEWS_ENDPOINT, BRAVE_SEARCH_ENDPOINT, BRAVE_SUMMARIZER_ENDPOINT, BRAVE_WEB_ENDPOINT, DEFAULT_EXTRACTION_SCHEMA, DEFAULT_LANGSMITH_ENDPOINT, DEFAULT_LANGSMITH_PROJECT, DEFAULT_LLM_MAX_REQUESTS, DEFAULT_LLM_TIME_WINDOW, DEFAULT_LLM_TIMEOUT, DEFAULT_MARKET_DATA_SOURCES, DEFAULT_MAX_CONCURRENT_CONNECTIONS, DEFAULT_MAX_CONNECTIONS_PER_HOST, DEFAULT_MAX_CONTENT_CHARS, DEFAULT_MAX_INFO_TOOL_CALLS, DEFAULT_MAX_LOOPS, DEFAULT_MAX_NEWS_RESULTS, DEFAULT_MAX_RESEARCH_ITERATIONS, DEFAULT_MAX_SEARCH_RESULTS, DEFAULT_MAX_SUMMARIZER_RESULTS, DEFAULT_MAX_VALIDATION_ATTEMPTS, DEFAULT_MODEL_NAME, DEFAULT_POSTGRES_DB, DEFAULT_POSTGRES_HOST, DEFAULT_POSTGRES_PORT, DEFAULT_POSTGRES_USER, DEFAULT_QDRANT_COLLECTION_NAME, DEFAULT_QDRANT_HOST, DEFAULT_QDRANT_PORT, DEFAULT_TEMPERATURE, DEFAULT_TOOL_TIMEOUT, DEFAULT_VALIDATION_THRESHOLDS, DEFAULT_WEB_MAX_REQUESTS, DEFAULT_WEB_TIME_WINDOW, AgentType
from agent.log_config import logger
from agent.utils.logging import error_highlight, info_highlight, info_success, warning_highlight
```

## Description
`Environment` and default settings.

This module provides functionality for environment and default settings
in the agent framework.

## Function: get_openai_api_key

```python
def get_openai_api_key() -> str
```

`Retrieve` `OpenAI` `API` key from environment.


**Returns:**

    `str`: API key value or empty string
    

**Environment:**

    `OPENAI_API_KEY`: Required API key
    

**Logs:**

    `Warning` if key not found

**Returns:**

- str

## Function: get_openai_api_base

```python
def get_openai_api_base() -> Optional[str]
```

`Get` `OpenAI` `API` base `URL` from environment variables.


**Returns:**

    `str`: Base URL for OpenAI API or None
    

**Environment:**

    `OPENAI_API_BASE_URL`: Optional API endpoint override
    

**Logs:**

    `Warning` if `URL` not found

**Returns:**

- Optional[str]

## Function: get_anthropic_api_key

```python
def get_anthropic_api_key() -> str
```

`Get` `Anthropic` `API` key from environment variables.


**Returns:**

    `str`: API key value or empty string
    

**Environment:**

    `ANTHROPIC_API_KEY`: Optional API key
    

**Logs:**

    `Success` message when key is found

**Returns:**

- str

## Function: get_fireworks_api_key

```python
def get_fireworks_api_key() -> str
```

`Get` `Fireworks` `API` key from environment variables.


**Returns:**

    `str`: API key value or empty string
    

**Environment:**

    `FIREWORKS_API_KEY`: Optional API key
    

**Logs:**

    `Success` message when key is found

**Returns:**

- str

## Function: get_brave_api_key

```python
def get_brave_api_key() -> str
```

`Get` `Brave` `API` key from environment variables.


**Returns:**

    `str`: API key value or empty string
    

**Environment:**

    `BRAVE_SEARCH_API_KEY`: Required API key
    

**Logs:**

    `Warning` if key not found

**Returns:**

- str

## Function: get_brave_search_endpoint

```python
def get_brave_search_endpoint() -> str
```

`Get` `Brave` `Search` `API` endpoint.


**Returns:**

    `str`: Configured endpoint or default constant
    

**Environment:**

    `BRAVE_SEARCH_ENDPOINT`: Optional override
    

**Default:**

    `Uses` `BRAVE_SEARCH_ENDPOINT` from constants

**Returns:**

- str

## Function: get_brave_web_endpoint

```python
def get_brave_web_endpoint() -> str
```

`Get` `Brave` web endpoint from environment variables.


**Returns:**

    `str`: Configured endpoint or default constant
    

**Environment:**

    `BRAVE_WEB_ENDPOINT`: Optional override
    

**Default:**

    `Uses` `BRAVE_WEB_ENDPOINT` from constants

**Returns:**

- str

## Function: get_brave_summarizer_endpoint

```python
def get_brave_summarizer_endpoint() -> str
```

`Get` `Brave` summarizer endpoint from environment variables.


**Returns:**

    `str`: Configured endpoint or default constant
    

**Environment:**

    `BRAVE_SUMMARIZER_ENDPOINT`: Optional override
    

**Default:**

    `Uses` `BRAVE_SUMMARIZER_ENDPOINT` from constants

**Returns:**

- str

## Function: get_brave_news_endpoint

```python
def get_brave_news_endpoint() -> str
```

`Get` `Brave` news endpoint from environment variables.


**Returns:**

    `str`: Configured endpoint or default constant
    

**Environment:**

    `BRAVE_NEWS_ENDPOINT`: Optional override
    

**Default:**

    `Uses` `BRAVE_NEWS_ENDPOINT` from constants

**Returns:**

- str

## Function: get_searxng_url

```python
def get_searxng_url() -> str
```

`Get` `SearXNG` `URL` from environment variables.


**Returns:**

    `str`: URL value or empty string
    

**Environment:**

    `SEARXNG_URL`: Optional URL
    

**Logs:**

    `Success` message when `URL` is found

**Returns:**

- str

## Function: get_qdrant_host

```python
def get_qdrant_host() -> str
```

`Retrieve` `Qdrant` vector database host.


**Returns:**

    `str`: Hostname/IP from environment or default
    

**Environment:**

    `QDRANT_HOST`: Optional override
    

**Default:**

    `DEFAULT_QDRANT_HOST` from constants

**Returns:**

- str

## Function: get_qdrant_port

```python
def get_qdrant_port() -> int
```

`Get` `Qdrant` port from environment variables.


**Returns:**

    `int`: Port number from environment or default
    

**Environment:**

    `QDRANT_PORT`: Optional override
    

**Default:**

    `DEFAULT_QDRANT_PORT` from constants

**Returns:**

- int

## Function: get_qdrant_collection_name

```python
def get_qdrant_collection_name() -> str
```

`Get` `Qdrant` collection name from environment variables.


**Returns:**

    `str`: Collection name from environment or default
    

**Environment:**

    `QDRANT_COLLECTION_NAME`: Optional override
    

**Default:**

    `DEFAULT_QDRANT_COLLECTION_NAME` from constants

**Returns:**

- str

## Function: get_langsmith_api_key

```python
def get_langsmith_api_key() -> str
```

`Get` `LangSmith` `API` key from environment variables.


**Returns:**

    `str`: API key value or empty string
    

**Environment:**

    `LANGSMITH_API_KEY`: Optional API key
    

**Logs:**

    `Success` message when key is found

**Returns:**

- str

## Function: get_langsmith_project

```python
def get_langsmith_project() -> str
```

`Get` `LangSmith` project name.


**Returns:**

    `str`: Project name from environment or default
    

**Environment:**

    `LANGSMITH_PROJECT`: Optional override
    

**Default:**

    `DEFAULT_LANGSMITH_PROJECT` from constants

**Returns:**

- str

## Function: get_langsmith_endpoint

```python
def get_langsmith_endpoint() -> str
```

`Get` `LangSmith` endpoint from environment variables.


**Returns:**

    `str`: Endpoint URL from environment or default
    

**Environment:**

    `LANGSMITH_ENDPOINT`: Optional override
    

**Default:**

    `DEFAULT_LANGSMITH_ENDPOINT` from constants

**Returns:**

- str

## Function: get_postgres_user

```python
def get_postgres_user() -> str
```

`Get` `PostgreSQL` user from environment variables.


**Returns:**

    `str`: Username from environment or default
    

**Environment:**

    `POSTGRES_USER`: Optional override
    

**Default:**

    `DEFAULT_POSTGRES_USER` from constants

**Returns:**

- str

## Function: get_postgres_password

```python
def get_postgres_password() -> str
```

`Retrieve` `PostgreSQL` database password.


**Returns:**

    `str`: Password value or empty string
    

**Environment:**

    `POSTGRES_PASSWORD`: Required for database access
    

**Logs:**

    `Warning` if password not found

**Returns:**

- str

## Function: get_postgres_db

```python
def get_postgres_db() -> str
```

`Get` `PostgreSQL` database from environment variables.


**Returns:**

    `str`: Database name from environment or default
    

**Environment:**

    `POSTGRES_DB`: Optional override
    

**Default:**

    `DEFAULT_POSTGRES_DB` from constants

**Returns:**

- str

## Function: get_postgres_host

```python
def get_postgres_host() -> str
```

`Get` `PostgreSQL` host from environment variables.


**Returns:**

    `str`: Hostname from environment or default
    

**Environment:**

    `POSTGRES_HOST`: Optional override
    

**Default:**

    `DEFAULT_POSTGRES_HOST` from constants

**Returns:**

- str

## Function: get_postgres_port

```python
def get_postgres_port() -> int
```

`Get` `PostgreSQL` port from environment variables.


**Returns:**

    `int`: Port number from environment or default
    

**Environment:**

    `POSTGRES_PORT`: Optional override
    

**Default:**

    `DEFAULT_POSTGRES_PORT` from constants

**Returns:**

- int

## Function: get_proxy_url

```python
def get_proxy_url() -> str
```

`Retrieve` network proxy `URL`.


**Returns:**

    `str`: Proxy URL in format protocol://host:port
    

**Environment:**

    `PROXY_URL`: Full proxy URL
    

**Logs:**

    `Info` message when proxy found

**Returns:**

- str

## Function: get_proxy_username

```python
def get_proxy_username() -> str
```

`Get` proxy username from environment variables.


**Returns:**

    `str`: Username value or empty string
    

**Environment:**

    `OXYLABS_USERNAME`: Optional proxy username

**Returns:**

- str

## Function: get_proxy_password

```python
def get_proxy_password() -> str
```

`Get` proxy password from environment variables.


**Returns:**

    `str`: Password value or empty string
    

**Environment:**

    `OXYLABS_PASSWORD`: Optional proxy password
    

**Logs:**

    `Warning` if password not found

**Returns:**

- str

## Function: get_jina_api_key

```python
def get_jina_api_key() -> str
```

`Retrieve` `Jina` `AI` `API` credentials.


**Returns:**

    `str`: API key or empty string
    

**Environment:**

    `JINA_API_KEY`: Required for Jina services
    

**Logs:**

    `Info` when found, warning when missing

**Returns:**

- str
