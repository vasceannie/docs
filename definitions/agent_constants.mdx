# constants

[code:agent/constants.py]

## Content

```python
"""Global constants and default configuration values.

This module defines all global constants, default values, and enumerations used throughout
the agent framework. It serves as the single source of truth for configuration defaults
and shared constants.

Key Components:
    - AgentType: Enumeration of supported agent types
    - API endpoints: Default URLs for external services
    - LLM settings: Default model parameters
    - Database defaults: Connection parameters for PostgreSQL and Qdrant...
```